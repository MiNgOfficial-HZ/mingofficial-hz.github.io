# MiNgHZ 的小站

> 一半是生活，一半是热爱。 · 参考风格：tzblog.tech（简洁、卡片式、温暖色调）

访问：**https://mingofficial-hz.github.io/**

## 功能模块

| 模块 | 说明 |
| --- | --- |
| 🏠 主页门户 | 说明「这是什么站」，并分成两扇门：左「MiNg 和他的朋友们」、右「MiNg 的个人空间」（`#home` / `#friends` / `#space` 可直链） |
| 💬 说说墙（朋友们） | 我发帖、朋友也能发帖；每条显示发帖人 + 北京时间 + 「刚刚 / N 分钟前」；每条可展开跟帖，跟帖显示跟帖人与时间；自己的帖 / 跟帖可删 |
| 📚 指南（朋友们） | 教程与学习笔记，卡片与阅读页都显示「✍️ 编写：XXX」；朋友可写自己的指南（可配图） |
| 🔗 友链 & 留言（朋友们） | 公开留言（邮箱仅站长可见）+ 站长站内回复；友链可增删改 |
| 🧳 旅行日志（个人空间） | 卡片网格展示游记，可增删改 |
| 📷 数码生活（个人空间） | 时间线展示数码产品体验与评分 |
| 🎧 Personal Devices（个人空间） | 一行两台展示个人设备（emoji + 类别 + 产品名），可增删改 |
| 📷 摄影空间（个人空间 → 三级页面 `/photo/`） | 独立页面：作品名 + 拍摄地点 + 拍摄时间 + 多张照片；只有站长能编辑，照片本地选取后自动压到 1080p 再上传，支持大图查看 |
| 🎲 桌游（朋友们 → 三级页面 `/games/`） | 桌游桌：收录阿瓦隆与血染钟楼，含两款游戏的对比表与「第一次玩选哪个」；从这一页进入各自的规则 / 角色 / 在线工具 |
| 🎲 阿瓦隆（桌游 → `/avalon/`） | 规则 + 9 个角色卡片 + 在线开局；人物详情 `/avalon/roles/<角色>/` |
| 🎭 血染钟楼（桌游 → `/botc/`） | 规则 + 22 个角色（暗流涌动）+ 在线工具：说书人配板发牌、夜序向导、魔典标记、白天提名处决、私密信息；角色详情 `/botc/roles/<角色>/` |
| 👤 账号与权限（个人空间） | 登录 / 改密 / 两步验证 / 管理面板（建号、重置密码、权限开关）、退出登录 —— 原来的账号管理系统整体搬到这里 |
| 📐 数学公式 | Markdown 里 `$…$` / `$$…$$` 用 KaTeX 渲染（本地懒加载，无公式就不下载）；另支持表格 / 任务清单 / 删除线 / 引用 |
| 📖 Markdown 速查 | 编辑器右侧 20 条常用符号（1~6 级标题 + 加粗 / 列表 / 公式 / 表格…），点一下插入光标处；正文标题支持到六级 |
| 👤 账号体系 | 站长发放账号密码；访客登录后按权限（可编辑/仅查看）呈现界面 |
| 🔐 两步验证（2FA） | 站长 / 管理员可自行开启：密码 + 验证器 App 动态码（TOTP）；附 8 枚一次性恢复码 |
| 🤖 人机验证 | Cloudflare Turnstile（Managed）保护「登录」与「留言」；未配置密钥时自动休眠 |
| 👤 关于页 | `/about/` —— 个人信息页（Base / 学校 / 方向 / 爱好 / 邮箱 / GitHub），导航栏「关于」进入 |
| 🛡️ 管理面板 | 新建账号、设置权限、授权/取消管理员、重置密码、删除 |
| 🔑 朋友账号权限 | 发留言 / 跟帖（默认开）· 发说说 · 写指南 · 编辑全部内容（高级）；只能改自己发的内容，作者与时间由服务端盖章 |

## 架构（v6）

```
静态页面（GitHub Pages，零密钥）
  ├─ 游客 / 访客 / 管理员：数据由 Worker 过滤（无邮箱、无用户表）
  └─ 站长 MiNgAltair：完整数据（留言邮箱）+ 管理面板
          ↓ 数据读写经 Cloudflare Worker（凭证仅存服务端）
私有 GitHub minghz-db/db.json（内容数据 + 账号数据[PBKDF2 哈希]）
图片经 Worker /api/img 代理读取（边缘缓存一年）；新留言邮件通知已停用
```

## 首屏与上传优化（v7）

- 字体非阻塞加载：Google Fonts 以 `media="print"` 低优先级拉取，加载完再切换，首屏先用系统字体渲染
- `/api/db` 一次请求同时返回数据与当前用户；Worker 侧数据读有 15 秒边缘缓存，写操作立即失效
- 首页先渲染 localStorage 缓存再请求云端（`requestAnimationFrame` 之后），断网也能立刻看到内容
- 图片：浏览器内 `createImageBitmap` 解码 + WebP 压缩（长边 1600，约 900KB 以内）+ XHR 进度 + 单张重试；一次多张会合并成一个 commit
- 人机验证：官方 onload 回调 + 失败兜底 UI（显示原因与「重新验证」），iPad 上不再把登录按钮卡死

## 账号与权限

- 站长：MiNgAltair（种子账号，请部署后立即修改初始密码；切勿把真实密码写进公开仓库）
- 角色：owner（站长）> admin（管理员）> member（访客）> 游客（未登录）
- 登录限流：每 IP 每小时 12 次尝试；密码 PBKDF2-SHA256（60000 迭代+随机盐）
- 人机验证：Turnstile 令牌由 Worker 调用 siteverify 服务端校验；校验服务不可达时放行，避免连坐正常用户
- 两步验证：TOTP（SHA-1 / 6 位 / 30 秒，±30 秒容差）；密钥与恢复码哈希只存在私有仓库，页面与接口都不下发
- 管理面板仅 owner 可创建/授权/删除；admin 可查看账号列表

## 安全管理

- 页面源码零密钥；minghz-db 为私有仓库，读取经 Worker 按角色剥除邮箱/用户表
- 登录/改密/发放账号全部经 Worker 服务端校验；上传图片服务端校验真实格式
- 开启两步验证后：登录要「密码 + 动态码」两步；改密也要再验一次动态码；关闭 / 重置恢复码同样需要动态码
- 忘记设备时：站长可在管理面板「重置两步验证」帮成员兜底，本人也可用一次性恢复码登录
- 所有数据变更走 git 提交历史，可回滚；内容修改需登录且具备可编辑权限

## 本地预览

    npx serve .  或直接双击 index.html（登录/管理需在线）
